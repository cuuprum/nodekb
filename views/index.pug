extends layout

block content
  h2 #{title}
  p
  ul
    each article, i in articles
      li= article.title 
      ='Author: ' + article.author
      br
      = 'Year : ' + article.year
      br
      = article.body
    
